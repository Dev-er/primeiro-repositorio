/**
 * Crie um algortimo que recebe informações de 15 produtos. 
 * Cada produto possui nome, marca, uma foto (link de uma imagem da internet), 
 * preço unitário e a quantidade de produtos em estoque. Ao receber 
 * essas informações dos produtos, imprima no HTML essas informações 
 * de cada produto e o produto entre o preço e a quantidade de produtos 
 * em estoque (preço total).
 */

const nums = []

let produto = {
    nome: '',
    marca: '',
    preco: faturamento,
    quantidade: inventario,
    link: '',
}
let inventario: {
    estoque: 0,
    precoUnitario: 0,
}

let faturamento: {
    precoFinal: 0,
}

for (let i = 1; i <= 15; i++){
    produto.nome(prompt(`Entre com o nome do produto que deseja ${i}°`))
    produto.marca(prompt(`Entre com a marca do produto que deseja ${i}°`))
    produto.preco(prompt(`Entre com o preço deste produto ${i}°`))
    produto.estoque(prompt(`Entre com o quantidade em estoque do produto que deseja ${i}°`))
    produto.nome(prompt(`Entre com o nome do produto que deseja ${i}°`))
    nums.push(n)
}


